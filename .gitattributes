# Normalize line endings
* text=auto

# Notebook diffs
*.ipynb diff=jupyternotebook

# No LFS — large models go to HuggingFace Hub, not git.
# The only binary we commit is the small LoRA adapter (~2 MB).
data/outputs/*.json -filter -diff -merge
